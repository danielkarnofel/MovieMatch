<!DOCTYPE html>
<html>
<%- include('partials/head.ejs') %>
<body>
    <main>
        <div class="d-flex justify-content-center align-items-center vh-100">
            <div class="text-center">
                <div class="container border border-secondary rounded p-4 bg-dark text-white">
                    <img src="img/logo.svg" class="mb-3" alt="Logo">
                    <h1>MovieMatch</h1>

                    <% if (isAuthenticated) { %>
                        <h2 class="text-success">You are already logged in!</h2>
                    <% } %>

                    <form action="/login" method="POST" class="mt-3">
                        <input type="text" name="username" placeholder="Username" class="form-control mb-2" required>
                        <input type="password" name="password" placeholder="Password" class="form-control mb-3" required>
                        <button class="btn btn-primary w-100">Login</button>
                    </form>

                    <a href="/signup" class="d-block mt-3 text-white text-decoration-none">Create account</a>
                </div>
            </div>
        </div>
    </main>
</body>
</html>